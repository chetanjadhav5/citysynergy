apiVersion: v1
kind: Service
metadata:
  name: citysynergy-backend
  namespace: citysynergy
spec:
  type: LoadBalancer
  selector:
    app: citysynergy-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

